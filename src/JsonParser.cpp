/**
 * OddSockets C++ SDK - JSON Parser
 * json::parse(), json::stringify(), and json::Value are implemented in Types.cpp
 * using a built-in minimal parser (no external dependency).
 *
 * For production, you can replace with nlohmann/json:
 *   vcpkg install nlohmann-json
 */
