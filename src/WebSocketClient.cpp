/**
 * OddSockets C++ SDK - WebSocket Client
 * WebSocketClient::create() is implemented in Types.cpp
 * using the LibWebSocketClient class.
 *
 * For production WebSocket transport, replace the LibWebSocketClient
 * in Types.cpp with a real implementation using:
 * - ixwebsocket (recommended): vcpkg install ixwebsocket
 * - libwebsockets: apt install libwebsockets-dev
 * - websocketpp: vcpkg install websocketpp
 */
